<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.administrator.dbmsproject">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".view.SelectAddressActivity"
            android:theme="@style/SelectAddressActivity"/>
        <activity android:name=".view.TrainScheduleActivity">
        </activity>
        <activity android:name=".view.SaleActivity">
        </activity>
        <activity android:name=".view.ShowTicketActivity">
        </activity>
        <activity android:name=".view.SeatInfoActivity">
        </activity>
    </application>

</manifest>